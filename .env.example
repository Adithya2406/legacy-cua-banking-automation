# Required for OpenAI. Leave blank only when using a trusted local compatible provider.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Optional OpenAI-compatible endpoint. Keep blank for OpenAI.
OPENAI_BASE_URL=
# Keep enabled for assignment-compliant visual discovery.
OPENAI_INCLUDE_SCREENSHOT=true
